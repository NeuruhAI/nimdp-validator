name: NIMDP Gate
on: [push, pull_request]
jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - run: python -m pip install --upgrade pip
      - run: pip install -r requirements.txt
      - run: |
          mkdir -p reports
          python validator.py --project-name "${{ github.sha }}" --input samples/demo_spec.md --outdir reports
      # Optional Notion push (requires org/repo secrets)
      # - run: |
      #     python validator.py --project-name "${{ github.sha }}" --input samples/demo_spec.md --outdir reports --push-notion
      #   env:
      #     NOTION_API_KEY: ${{ secrets.NOTION_API_KEY }}
      #     NOTION_DATABASE_ID: ${{ secrets.NOTION_DATABASE_ID }}
